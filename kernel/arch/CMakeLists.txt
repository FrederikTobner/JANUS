file(GLOB_RECURSE ARCH_SPECIFIC_SOURCES
    "${CMAKE_CURRENT_SOURCE_DIR}/${TINYOS_TARGET_ARCH}/*.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/${TINYOS_TARGET_ARCH}/*.asm"
)
file(GLOB_RECURSE ARCH_SPECIFIC_HEADERS
    "${CMAKE_CURRENT_SOURCE_DIR}/${TINYOS_TARGET_ARCH}/*.h"
)
file(GLOB_RECURSE ARCH_COMMON_HEADERS
    "${CMAKE_CURRENT_SOURCE_DIR}/include/arch/*.h"
)
 
tinyos_add_library(arch
    SOURCES
        ${ARCH_SPECIFIC_SOURCES}
        ${ARCH_SPECIFIC_HEADERS}
        ${ARCH_COMMON_HEADERS}
)
