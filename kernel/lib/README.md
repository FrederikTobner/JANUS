# JANUS Freestanding Kernel Libraries

This directory contains utility libraries used by the JANUS kernel. Each library is a separate module with clear responsibilities. E.g. String an memory operations, data structures. compression, decompression, checksum calculation, hashing, math or general algorithms.
