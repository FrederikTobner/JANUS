# CMake configuration for the display library
#
# Header-only library providing the shared display_info_t type
# used by both the boot subsystem and the drivers subsystem.

janus_add_library(display)
