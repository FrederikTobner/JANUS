# aarch64 interrupts 

The aarch64 implementation is a stub for now, as the kernel is still in early development. 
The interrupt handling will be implemented in the future, and the current implementation is just a placeholder to allow the kernel to compile and run on aarch64 systems.
