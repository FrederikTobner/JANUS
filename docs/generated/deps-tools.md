> *Generated at configure time — re-run `cmake -B build-tools` to regenerate.*

```mermaid
graph TD
  subgraph tool_libraries["Tool Libraries"]
    clap("clap")
  end
  subgraph tools["Tools"]
    psf2c{{"psf2c"}}
  end
  psf2c --> clap
```
