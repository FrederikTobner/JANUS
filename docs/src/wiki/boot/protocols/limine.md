# Limine

The limine  boot protocol is a modern, flexible boot specification designed for x86_64 PCs. It defines how a kernel can be loaded by a compliant bootloader, and how boot-time information is passed to the kernel. Limine is supported by the Limine Bootloader, which can be used to load JANUS in both legacy BIOS and UEFI modes.


