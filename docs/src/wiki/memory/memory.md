# Memory

How RAM is managed in an operating system — from raw physical pages to
virtual address spaces and the page tables that translate between them.

* [Physical Memory](physical-memory.md) — page frames, the memory map, and how the kernel tracks free RAM
* [Virtual Memory](virtual-memory.md) — address spaces, page tables, and the higher half kernel
