# Unit tests
project(tinyos-unit-tests)

# Unit test executables will be added here as they are created
# Example:
# add_executable(test_buffer test_buffer.c)
# target_link_libraries(test_buffer kbuffer)
# add_test(NAME test_buffer COMMAND test_buffer)
